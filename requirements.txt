# Core
numpy>=1.26
pandas>=2.2
pyarrow>=15.0
scipy>=1.13
scikit-learn>=1.5
pyyaml>=6.0
tqdm>=4.66
python-dotenv>=1.0
joblib>=1.4

# Statistics / causal inference
statsmodels>=0.14
econml>=0.15           # DML cross-fit DR
zEpid>=0.9.0           # TMLE
dowhy>=0.11            # mediation analysis

# Off-policy evaluation
obp>=0.5.7             # MIPS, IPS, DR estimators (Open Bandit Pipeline)

# Embeddings / NLP
torch>=2.3
transformers>=4.43
sentence-transformers>=3.0
faiss-cpu>=1.8         # nearest-neighbour positivity diagnostics

# LLM API clients
anthropic>=0.34
openai>=1.40
google-generativeai>=0.7

# Distance / divergence
geomloss>=0.2.6        # MMD / sinkhorn for positivity diagnostics

# Reproducibility / experiment tracking
hydra-core>=1.3
wandb>=0.17

# Dev
pytest>=8.2
pytest-cov>=5.0
ruff>=0.5
black>=24.4
ipython>=8.25
jupyter>=1.0

# Schema validation (pi_agent JSON output)
pydantic>=2.7
jsonschema>=4.22
